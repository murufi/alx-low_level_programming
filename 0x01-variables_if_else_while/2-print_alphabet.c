#include <stdio.h>
/**
 * main -  Entery point
 * Discripton: 'Check for number if it is positive or negative'
 * Return: 'always 0'
 */
int main(void)
{
   int n = 97;
   while (n <= 122)
   {
	   putchar(n);
	   n++;
   }
   putchar('\n');

    return (0);
}
