#include <stdio.h>
/**
 * main -  Entery point
 * Discripton: 'Check for number if it is positive or negative'
 * Return: 'always 0'
 */
int main(void)
{
	int n;

	for (n = 0; n <= 0; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
